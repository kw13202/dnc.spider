{
  "Logging": {
    // 日志等级 Trace < Debug < Information < Warning < Error < Critical < None
    "LogLevel": {
      // 日志等级 Trace < Debug < Information < Warning < Error < Critical < None
      "LogLevel": {
        // 默认级别
        "Default": "Debug",
        // 命名空间是dnc（本项目）级别
        "dnc": "Trace",
        // 命名空间是System级别
        "System": "Warning",
        // 命名空间是Microsoft级别
        "Microsoft": "Warning"
      }
    }
  }
}
